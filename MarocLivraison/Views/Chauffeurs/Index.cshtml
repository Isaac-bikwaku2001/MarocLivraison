@model IEnumerable<MarocLivraison.Models.Chauffeur>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CIN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomComplet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumPermis)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CIN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomComplet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumPermis)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChauffeurID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChauffeurID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChauffeurID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
